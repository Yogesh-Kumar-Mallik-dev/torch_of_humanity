# .gitignore

## Purpose

Defines repository ignore rules for generated or local-only paths.

## Current Rules

- `assets/`

## Impact

- Prevents all assets content from being tracked in git.
- Keeps large/generated art and map content local to development environments.
